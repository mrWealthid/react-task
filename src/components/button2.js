// import PropTypes from "prop-types";

// const Button = ({ color, text, click }) => {
//   return (
//     <span>
//       <button onClick={click} style={{ background: color }}>
//         {text}
//       </button>
//     </span>
//   );
// };

// Button.defaultProps = {
//   color: "cadetblue",
// };

// Button.propTypes = {
//   text: PropTypes.string,
//   color: PropTypes.string,
//   onClick: PropTypes.func,
// };

// export default Button;
